// Tailwind v4 is handled by the @tailwindcss/vite plugin (see vite.config.ts),
// so no PostCSS plugins are needed here. This file exists only to stop PostCSS
// from walking up the directory tree and picking up an unrelated parent config.
export default { plugins: {} };
